# The Vite dev server proxies /api requests to the Python server
# automatically (see vite.config.ts). No VITE_API_URL is needed.
# If you need to override the API target, set VITE_API_URL here.
# VITE_API_URL=
